# CSV Data Cleaner (R)
Simple CLI R script to clean CSV files.

## Requirements
- R (>= 3.6)

## Usage
```bash
Rscript main.R input.csv output.csv
```
The cleaner will trim strings, remove empty rows, and drop duplicate rows.
